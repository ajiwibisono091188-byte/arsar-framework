# Public Directory

Folder ini berisi aset statis yang akan disalin secara langsung (tanpa diolah/bundle) oleh Vite ke root folder hasil build (`src/dist/`).
Cocok untuk file seperti:
- `favicon.ico`
- `robots.txt`
- `sitemap.xml`
