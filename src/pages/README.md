# Pages Directory

Direktori ini berisi halaman-halaman HTML statis hasil kompilasi dari script generator (misal: halaman landing per kota, halaman artikel, dll).
Vite secara otomatis mendeteksi semua file `.html` di dalam direktori ini (dan direktori `src/` lainnya) untuk dijadikan entrypoint build bundler Rollup.
